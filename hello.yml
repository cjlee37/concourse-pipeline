jobs:
- name: hello-world
  plan:
  - task: say-hello
    config:
      platform: linux
      image_resource:
        type: docker-image
        source: {repository: ubuntu}
      run:
        path: echo
        args: ["Hello, world!"]

resources:
- name: concourse-pipeline 
  type: git
  source:
    uri: http://github.com/cjlee37/concourse-pipeline.git
    branch: master
